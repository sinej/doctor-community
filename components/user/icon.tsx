'use client'

interface UserIconType {

};

const UserIcon = (props: UserIconType) => {
    const {} = props;
    return (
        <>
            UserIcon
        </>
    );
}

export default UserIcon;